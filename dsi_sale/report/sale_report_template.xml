<odoo>
    <template id="dsi_report_saleorder_document" inherit_id="sale.report_saleorder_document" priority="1">
        <xpath expr="//thead//tr" position="inside">
            <th name="th_section_subtotal" class="text-right">
                <span groups="account.group_show_line_subtotals_tax_excluded">Section Total</span>
            </th>
        </xpath>

        <xpath expr="//tbody" position="replace">
            <!-- Odoo Standard -->
            <tbody class="sale_tbody">

                <t t-set="current_subtotal" t-value="0"/>

                <t t-foreach="doc.order_line" t-as="line">

                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"
                       groups="account.group_show_line_subtotals_tax_excluded"/>
                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_total"
                       groups="account.group_show_line_subtotals_tax_included"/>
            <!-- Odoo Standard -->

                    <!-- Custo -->
                    <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else 'bg-100 font-weight-bold font-italic o_line_sub_section' if line.display_type == 'line_sub_section' else ''">
                    <!-- Custo -->

            <!-- Odoo Standard -->
                        <t t-if="not line.display_type">
                            <td name="td_name">
                                <span t-field="line.name"/>
                            </td>
                            <td name="td_quantity" class="text-right">
                                <span t-field="line.product_uom_qty"/>
                                <span t-field="line.product_uom"/>
                            </td>
                            <td name="td_priceunit" class="text-right">
                                <span t-field="line.price_unit"/>
                            </td>
                            <td t-if="display_discount" class="text-right"
                                groups="product.group_discount_per_so_line">
                                <span t-field="line.discount"/>
                            </td>
                            <td name="td_taxes" class="text-right">
                                <span t-esc="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                            </td>
                            <td name="td_subtotal" class="text-right o_price_total">
                                <span t-field="line.price_subtotal"
                                      groups="account.group_show_line_subtotals_tax_excluded"/>
                                <span t-field="line.price_total"
                                      groups="account.group_show_line_subtotals_tax_included"/>
                            </td>
                            <td name="td_section_subtotal" class="text-right o_price_total">
                                <span/>
                            </td>
                        </t>
                        <t t-if="line.display_type == 'line_note'">
                            <td name="td_note_line" colspan="99">
                                <span t-field="line.name"/>
                            </td>
                        </t>
                        <!-- Odoo Standard -->

                        <!-- Custo -->
                        <t t-if="line.display_type == 'line_section'">
                            <td name="td_section_line" colspan="0">
                                <span t-field="line.name"/>
                            </td>
                            <td name="td_space" class="text-right">
                                <span/>
                            </td>
                            <td name="td_space" class="text-right">
                                <span/>
                            </td>
                            <td name="td_space" class="text-right">
                                <span/>
                            </td>
                            <td name="td_space" class="text-right">
                                <span/>
                            </td>

                            <td name="td_section_subtotal" class="text-right o_price_total">
                                <span t-field="line.total_section"
                                      groups="account.group_show_line_subtotals_tax_excluded"
                                      t-options='{"widget": "monetary", "display_currency": doc.pricelist_id.currency_id}'/>
                            </td>
                        </t>
                        <t t-if="line.display_type == 'line_sub_section'">
                            <td style="padding-left: 20px;" name="td_sub_section_line" colspan="99">
                                <span t-field="line.name"/>
                            </td>
                        </t>
                        <!-- Custo -->

                    </tr>
                </t>
            </tbody>
        </xpath>
    </template>
</odoo>
